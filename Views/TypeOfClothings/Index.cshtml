@model IEnumerable<WardrobeProject.Models.TypeOfClothing>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ClothingType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ClothingType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TypeOfClothingID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TypeOfClothingID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TypeOfClothingID })
        </td>
    </tr>
}

</table>
